template<auto N>
constexpr auto nth_pack_element(auto... args) {
  return args...[N];
}

int main() {
#if defined(METABENCH)
<% (0..n-1).each do |i| %>
    nth_pack_element<<%= i %>>(<%= (1..n).to_a.join(', ') %>);
<% end %>
#endif
}
